
export type InitialSheet = {
  __EMPTY: string;
  act_type?: string;
  operDay?: string;
  serv_id?: string;
  time?: string;
}

export type NewSheet = {
  OPERDAY: string;
  SUIP: string;
  STATE: string;
  NOM_OPER: string;
  Requisite?: string;
  SUM?: string;
}